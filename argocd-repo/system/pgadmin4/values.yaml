ingress:
  enabled: true
  ingressClassName: nginx
  hosts:
    - host: pgadmin4.localhost
      paths:
        - path: /
          pathType: Prefix

persistentVolume:
  enabled: false

env:
  email: admin
  password: password
